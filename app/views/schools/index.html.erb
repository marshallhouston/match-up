<% @schools.each do |school| %>
  <div class="school">
    <h3 class='h1 h0-responsive caps mt4 mb0 regular'><%= school.name %></h3>
    <h5 class='h4 h0-responsive caps mt4 mb0 regular'>Minimum Admission Score: <%= school.min_admission_score %></h5>
  </div>
<% end %>
